---
import Base from "../layouts/Layout.astro";
import HeroImage from "../assets/hero-2.avif";
import { Image } from "astro:assets";
import IntroImage from "../assets/intro-accueil.avif";
---

<Base titre="CONTACT - Ça va Trail'er">
    <div class="mt-100">
        <section
            class="relative w-full h-[90vh] flex items-center justify-center text-center text-white bg-cover bg-center overflow-hidden"
        >
            <Image
                src={HeroImage}
                alt="Paysage naturel avec randonneurs"
                width={1920}
                height={1080}
                class="absolute inset-0 w-full h-full object-cover object-center md:object-top"
            />

            <div
                class="absolute inset-0 bg-black bg-opacity-20 backdrop-blur-lg"
            >
            </div>

            <div class="relative z-10 px-8 max-w-4xl">
                <h1
                    class="text-6xl md:text-8xl mt-10 font-extrabold uppercase tracking-wide"
                >
                    Contactez-nous
                </h1>
                <p
                    class="mt-4 text-2xl md:text-3xl font-semibold text-orange-500 bg-white px-4 py-2 rounded-md inline-block"
                >
                    Nous sommes là pour vous aider
                </p>
            </div>
        </section>

        <!-- Conteneur principal -->
        <section class="max-w-4xl mx-auto my-12 px-6 text-center">
            <h1 class="text-3xl font-bold text-orange-600 mb-4">
                NOUS CONTACTER
            </h1>
            <p class="text-lg text-gray-700 mb-8">
                Vous souhaitez en savoir plus sur le festival ou poser une
                question ? Remplissez le formulaire ci-dessous ou contactez-nous
                directement !
            </p>

            <div class="flex flex-col md:flex-row items-start gap-10">
                <!-- Formulaire de contact -->
                <form
                    class="flex-1 space-y-4 bg-white shadow-lg p-6 rounded-lg"
                >
                    <div>
                        <label for="name" class="block font-bold text-gray-700"
                            >Nom</label
                        >
                        <input
                            type="text"
                            id="name"
                            name="name"
                            placeholder="Votre nom"
                            class="w-full px-4 py-2 border border-orange-400 rounded-md focus:ring-2 focus:ring-orange-500"
                        />
                    </div>

                    <div>
                        <label for="email" class="block font-bold text-gray-700"
                            >Email</label
                        >
                        <input
                            type="email"
                            id="email"
                            name="email"
                            placeholder="Votre email"
                            class="w-full px-4 py-2 border border-orange-400 rounded-md focus:ring-2 focus:ring-orange-500"
                        />
                    </div>

                    <div>
                        <label
                            for="message"
                            class="block font-bold text-gray-700">Message</label
                        >
                        <textarea
                            id="message"
                            name="message"
                            rows="4"
                            placeholder="Votre message..."
                            class="w-full px-4 py-2 border border-orange-400 rounded-md focus:ring-2 focus:ring-orange-500"
                        ></textarea>
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-orange-500 text-white font-bold py-2 rounded-md transition duration-300 hover:bg-orange-600"
                    >
                        Envoyer
                    </button>
                </form>

                <!-- Informations de contact -->
                <div
                    class="flex-1 bg-gray-100 p-6 rounded-lg shadow-lg text-left"
                >
                    <h3 class="text-orange-600 text-lg font-bold mb-2">
                        📍 Adresse
                    </h3>
                    <p class="text-gray-700">
                        123 Rue du Festival, 75000 Paris
                    </p>

                    <h3 class="text-orange-600 text-lg font-bold mt-4 mb-2">
                        📞 Téléphone
                    </h3>
                    <p class="text-gray-700">+33 1 23 45 67 89</p>

                    <h3 class="text-orange-600 text-lg font-bold mt-4 mb-2">
                        📧 Email
                    </h3>
                    <p class="text-gray-700">contact@cavatrailer.fr</p>
                </div>
            </div>
        </section>
    </div>
</Base>
